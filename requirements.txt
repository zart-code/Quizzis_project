# ============================================================
# Core (runtime)
# ============================================================
Django==5.1.4

# Real-time: WebSocket / ASGI стек
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.2
# redis-py 8.x ломает блокирующий BZPOPMIN в channels-redis (socket read
# timeout срабатывает раньше brpop_timeout → постоянные reconnect'ы).
# Держим совместимую 5.x ветку.
redis==5.2.1

# ============================================================
# Dev / CI: документация, линтеры, тесты
# ============================================================
sphinx
pycodestyle==2.13.0
pylint==3.3.6
coverage==7.8.0
black==26.5.1
